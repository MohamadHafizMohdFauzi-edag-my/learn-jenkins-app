pipeline {
    agent any // Run this pipeline on any available agent

    stages {

        stage('Build Docker Image') {
            steps {
                // Build a Docker image named 'my-playwright' using the Dockerfile in the current directory
                sh 'docker build -f ci/Dockerfile -t my-playwright .'
            }
        }
    }
}